# Ambiente (production ou development)
NODE_ENV=production

# Porta do servidor (o Render define automaticamente)
PORT=3000

# URL do frontend para CORS (em produção, use a URL do Vercel)
# Exemplo: https://kds-autoatendimento.vercel.app
FRONTEND_URL=http://localhost:5173
